# Practical lesson pz-SOLID  
# Практична реалізація SOLID принципів  

> У цьому занятті студенти отримуютьпрактичні навички застосування SOLID принципів під час рефакторингу існуючого коду.  
> Мета — створити гнучку, масштабовану та чисту архітектуру шляхом застосування SRP, OCP, LSP, ISP та DIP.

---

## Завдання виконано ✅

* ✅ Провести аналіз вихідного «анти-SOLID» коду  
* ✅ Визначити порушення кожного SOLID принципу  
* ✅ Виконати рефакторинг згідно з усіма 5 принципами
* ✅ Створити відповідні інтерфейси й абстракції  
* ✅ Усунути зайві залежності  
* ✅ Додати unit-тести для рефакторованого коду  
* ✅ Оформити звіт у Markdown  

---

## Структура проекту

```
Vishchun-SOLID/
├── src/
│   ├── index.ts                  # Демонстраційний приклад
│   ├── original/
│   │   └── UserManager.ts        # Анти-SOLID код із порушеннями
│   ├── refactored/
│   │   └── index.ts              # Рефакторований код (SOLID)
│   └── interfaces/
│       └── index.ts              # Інтерфейси й абстракції
├── tests/
│   └── refactored.spec.ts        # Unit тести
├── package.json
├── tsconfig.json
├── jest.config.js
├── .editorconfig
├── .gitignore
└── README.md
```

---

## Аналіз вихідного коду (анти-SOLID)

### 1. ❌ SRP - Single Responsibility Principle (Принцип єдиної відповідальності)

**Порушення:** Клас `UserManager` робить занадто багато:
- Управління користувачами (анти-паттерн)
- Відправлення email сповіщень
- Відправлення SMS сповіщень
- Генерування користувацьких звітів
- Генерування звітів активності
- Експорт до XML та JSON
- Резервне копіювання до хмари

**Наслідок:** Складно тестувати, потрібно константно змінювати клас, велика відповідальність.

**Приклад:**
```typescript
// ❌ ЛОШ: Один клас для всього
export class UserManager {
  addUser() { /*...*/ }
  removeUser() { /*...*/ }
  notifyUserByEmail() { /*...*/ }
  notifyUserBySms() { /*...*/ }
  generateUserReport() { /*...*/ }
  generateActivityReport() { /*...*/ }
  exportUsersToXml() { /*...*/ }
  exportUsersToJson() { /*...*/ }
  backupToCloud() { /*...*/ }
}
```

---

### 2. ❌ OCP - Open/Closed Principle (Принцип відкритості/закритості)

**Порушення:** Код замкнений для розширення:
- Для додавання нового типу сповіщення потрібно редагувати `UserManager`
- Для додавання нового типу експорту потрібно редагувати `UserManager`
- Для додавання нового типу звіту потрібно редагувати `UserManager`

**Наслідок:** Будь-яка нова функціональність вимагає змін у існуючому коді, що призводить до багів.

**Приклад:**
```typescript
// ❌ ЛОШ: Для нового типу сповіщення потрібно редагувати UserManager
notifyUserByEmail() { /*...*/ }
notifyUserBySms() { /*...*/ }
// Що якщо потрібен Push? Редагуємо UserManager!
```

---

### 3. ❌ LSP - Liskov Substitution Principle (Принцип підстановки Лісков)

**Порушення:** Клас `AdminUserManager` порушує контракт батьківського класу:

```typescript
// ❌ ЛОШ: Дочірній клас змінює сигнатуру методу
export class AdminUserManager extends UserManager {
  // Батько: addUser(id, name, email, phone)
  // Дочірній: addUser(id, name, email, phone, role) + кидає помилку якщо role !== 'admin'
  addUser(id, name, email, phone, role = 'admin') {
    if (role !== 'admin') {
      throw new Error('AdminUserManager може додавати тільки адміністраторів');
    }
    super.addUser(id, name, email, phone);
  }
}
```

**Наслідок:** Код, що використовує `UserManager`, не може безпечно використовувати `AdminUserManager`.

---

### 4. ❌ ISP - Interface Segregation Principle (Принцип розділення інтерфейсів)

**Порушення:** Великий клас з методами, які деякі клієнти не використовують:
- Якщо вам потрібен експорт, ви також отримуєте методи бекапу та сповіщень
- Якщо вам потрібна відправка email, ви також отримуєте методи для SMS
- Клієнти залежать від методів, які вони не використовують

**Наслідок:** Складно розуміти, які методи клас фактично використовує.

---

### 5. ❌ DIP - Dependency Inversion Principle (Принцип інверсії залежностей)

**Порушення:** Залежність від конкретних класів замість абстракцій:
- `UserManager` жорстко залежить від себе
- Немає інтерфейсів для абстракції
- Неможливо провести модульне тестування через mock'ування

**Наслідок:** Складно тестувати, складно змінити реалізацію.

---

## Рефакторований код (SOLID)

### 1. ✅ SRP - Single Responsibility Principle

**Рішення:** Розділяємо `UserManager` на окремі класи:

```typescript
// ✅ ДОБРЕ: Кожен клас має одну відповідальність
export class UserRepository implements IUserRepository {
  // Тільки управління користувачами
}

export class EmailNotificationSender implements IEmailNotificationSender {
  // Тільки отправка email сповіщень
}

export class SmsNotificationSender implements ISmsNotificationSender {
  // Тільки отправка SMS сповіщень
}

export class UserReportGenerator implements IUserReportGenerator {
  // Тільки генерування звітів користувачів
}

export class NotificationRepository implements INotificationRepository {
  // Тільки логування сповіщень
}
```

**Переваги:**
- Кожен клас легко розуміє його призначення
- Легко тестувати
- Легко змінювати

---

### 2. ✅ OCP - Open/Closed Principle

**Рішення:** Код відкритий для розширення через нові класи:

```typescript
// ✅ ДОБРЕ: Додаємо новий тип сповіщення без редагування старих
export class PushNotificationSender implements INotificationSender {
  send(userId: number, message: string): void {
    const user = this.userRepository.findUser(userId);
    if (user) {
      this.sendPush(user.id, message);
    }
  }
}

// ✅ ДОБРЕ: Додаємо новий тип експорту без редагування старих
export class CsvUserExporter implements IExporter {
  export(): string {
    // CSV експорт
  }
}
```

**Переваги:**
- Lahko dodajemo nove funkcionalnosti bez menjanja starog koda
- Smanjen je rizik od bagova
- Kod je fleksibilniji

---

### 3. ✅ LSP - Liskov Substitution Principle

**Рішення:** Правильна ієрархія спадкування без порушення контракту:

```typescript
// ✅ ДОБРЕ: AdminUserRepository розширює UserRepository правильно
export class AdminUserRepository extends UserRepository {
  // Наслідує всі методи батька без змін
  
  // Додає нові адміністративні методи
  deleteAllUsers(): void {
    const users = this.getAllUsers();
    users.forEach(u => this.removeUser(u.id));
  }
}

// ✅ ДОБРЕ: Можна використовувати AdminUserRepository замість UserRepository
const manageUsers = (repo: UserRepository) => {
  repo.addUser(user);
  repo.findUser(id); // Обидва класи працюють однаково
};

manageUsers(new UserRepository());
manageUsers(new AdminUserRepository()); // Працює без проблем!
```

**Переваги:**
- Наслідники повністю заміщають батьків
- Код, написаний для батька, працює й для нащадків
- Немає неочікуваних помилок

---

### 4. ✅ ISP - Interface Segregation Principle

**Рішення:** Розділяємо великі інтерфейси на малі й специфічні:

```typescript
// ✅ ДОБРЕ: Мінімальний інтерфейс для отправлення сповіщень
export interface INotificationSender {
  send(userId: number, message: string): void;
}

// ✅ ДОБРЕ: Специфічні інтерфейси для різних типів
export interface IEmailNotificationSender extends INotificationSender {
  sendEmail(email: string, message: string): void;
}

export interface ISmsNotificationSender extends INotificationSender {
  sendSms(phone: string, message: string): void;
}

// ✅ ДОБРЕ: Кожен клас імплементує тільки потрібні йому інтерфейси
export class EmailNotificationSender implements IEmailNotificationSender {
  // Не потрібно імплементувати SMS методи
}
```

**Переваги:**
- Клієнти залежать тільки від методів, які вони використовують
- Менше ненужних залежностей
- Більше гнучкості

---

### 5. ✅ DIP - Dependency Inversion Principle

**Рішення:** Залежності передаються через конструктор (Dependency Injection):

```typescript
// ✅ ДОБРЕ: EmailNotificationSender залежить від інтерфейсів
export class EmailNotificationSender implements IEmailNotificationSender {
  constructor(
    private userRepository: IUserRepository,  // Залежить від інтерфейсу, не класу
    private notificationRepository: INotificationRepository
  ) {}

  send(userId: number, message: string): void {
    const user = this.userRepository.findUser(userId);
    if (user) {
      this.sendEmail(user.email, message);
    }
  }
}

// ✅ ДОБРЕ: Легко замінити реалізацію для тестування
const mockUserRepository = {
  findUser: (id) => ({ id, email: 'test@example.com' }),
  getAllUsers: () => [],
  addUser: () => {},
  removeUser: () => {},
};

const emailSender = new EmailNotificationSender(
  mockUserRepository as any,
  new NotificationRepository()
);
```

**Переваги:**
- Легко тестувати
- Легко замінити реалізацію
- Код слабо пов'язаний

---

## Демонстрація SOLID в дії

### Додавання нового типу сповіщення (OCP)

Без редагування існуючого коду, ми можемо додати новий тип:

```typescript
// ✅ Новий клас - немає редагування старих
export class TelegramNotificationSender implements INotificationSender {
  constructor(
    private userRepository: IUserRepository,
    private notificationRepository: INotificationRepository
  ) {}

  send(userId: number, message: string): void {
    const user = this.userRepository.findUser(userId);
    if (user) {
      this.sendTelegram(user.id, message);
    }
  }

  private sendTelegram(userId: number, message: string): void {
    const notification = {
      type: 'telegram',
      userId,
      message,
      timestamp: new Date(),
    };
    this.notificationRepository.save(notification);
    console.log(`Telegram сповіщення відправлено користувачу ${userId}`);
  }
}
```

---

## Запуск проекту

### 1. Встановлення залежностей
```bash
npm install
```

### 2. Компіляція TypeScript
```bash
npm run build
```

### 3. Запуск демонстрації
```bash
npm start
```

### 4. Запуск тестів
```bash
npm test
```

---

## Unit Тести

Проект включає 20+ unit тестів, що покривають:

- **SRP:** Кожен клас має одну відповідальність
- **OCP:** Добавлення нових типів без редагування
- **LSP:** Наслідки правильно розширюють батьків
- **ISP:** Невеликі й специфічні інтерфейси
- **DIP:** Залежності від інтерфейсів
- **Інтеграційні тести:** Повний цикл роботи системи

Усі тести проходять успішно ✅

---

## Ключові вивчення

### Що таке SOLID?

SOLID - це 5 принципів об'єктно-орієнтованого проектування, які допомагають створити:
- **Гнучкий** код
- **Масштабований** код
- **Чистий** та **зрозумілий** код
- **Легко тестуємий** код

### 5 SOLID принципів:

1. **S** - Single Responsibility Principle (Принцип єдиної відповідальності)
2. **O** - Open/Closed Principle (Принцип відкритості/закритості)
3. **L** - Liskov Substitution Principle (Принцип підстановки Лісков)
4. **I** - Interface Segregation Principle (Принцип розділення інтерфейсів)
5. **D** - Dependency Inversion Principle (Принцип інверсії залежностей)

---

## Порівняння: До і Після

### Додавання нового типу сповіщення

**❌ ДО (Анти-SOLID):**
```
1. Відкриваємо UserManager
2. Редагуємо клас
3. Додаємо новий метод notifyUserBy[NewType]()
4. Потрібно тестувати весь клас заново
5. Ризик сломати існуючу функціональність
```

**✅ ПІСЛЯ (SOLID):**
```
1. Створюємо новий клас NewTypeNotificationSender
2. Імплементуємо інтерфейс INotificationSender
3. Готово! Без змін у існуючому коді
4. Тестуємо тільки новий клас
5. Немає ризику для існуючого коду
```

---

## Вик resources

- [SOLID Principles Explained](https://www.baeldung.com/solid-principles)
- [SOLID: The First 5 Principles of Object-Oriented Design](https://www.freecodecamp.org/news/solid-principles-explained-in-plain-english/)
- [JavaScript SOLID: Реалізація принципів](https://khalilstemmler.com/articles/solid-principles/)
- [Clean Code Concepts Adapted for JavaScript](https://github.com/ryanmcdermott/clean-code-javascript)
- [Dependency Injection in JavaScript](https://javascript.plainenglish.io/dependency-injection-in-javascript-1b82a8101c1a)

---

## Файли проекту

| Файл | Опис |
|------|------|
| `src/original/UserManager.ts` | Анти-SOLID код із порушеннями |
| `src/refactored/index.ts` | Рефакторований SOLID код |
| `src/interfaces/index.ts` | Інтерфейси й абстракції |
| `src/index.ts` | Демонстраційний приклад |
| `tests/refactored.spec.ts` | Unit тести |
| `package.json` | Залежності проекту |
| `tsconfig.json` | Конфігурація TypeScript |
| `jest.config.js` | Конфігурація Jest |

---

## Висновок

Цей проект демонструє, як застосування SOLID принципів робить код:
- **Більш гнучким** - легко додавати нові функціональності
- **Більш надійним** - менше багів
- **Більш тестуємим** - легко писати тести
- **Більш зрозумілим** - код повинен бути прочитаним
- **Більш масштабуємим** - можна легко розширювати систему

🎓 Результат: **SOLID код - це основа якісного ПО!**




